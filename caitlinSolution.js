var game = {
  // Choose word

  // Split word into an array

  // Take letter input

  // Store letter input in guess array

  // Check guesses array with letters array

  // End game if 5 wrong guesses or guesses.length === word.length
}
